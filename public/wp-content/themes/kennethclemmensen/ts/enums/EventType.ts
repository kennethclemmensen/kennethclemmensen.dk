/**
 * The EventType enum defines the event types
 */
export enum EventType {
    Click = 'click',
    DOMContentLoaded = 'DOMContentLoaded',
    Error = 'error',
    Keydown = 'keydown',
    Keypress = 'keypress',
    Load = 'load'
}